/* 
Mock helper file, which can handle action on app database
Those actions can be adding and removing users, which can be useful for testing registration

As we know nothing about database, this file will keep just functions indicating potential operations*/

export const deleteUserFromDB = (username) => {

}

export const addUserToDB = (username, password) => {
    
}

export const changeUserPrivileges = (username, newAccountType) => {
    
}